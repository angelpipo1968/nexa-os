services:
  - type: web
    name: nexa-backend-fastapi
    env: python
    buildCommand: pip install -r backend_fastapi/requirements.txt
    startCommand: uvicorn backend_fastapi.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
